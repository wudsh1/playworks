# 手部位置调试指南

## 🔍 快速诊断步骤

### Step 1: 运行游戏并查看调试信息

1. **运行游戏**
2. **查看控制台**，应该会自动输出详细调试信息
3. **找到手部引导对象**，右键 → `打印调试信息`

### Step 2: 查看 Scene 视图

1. **打开 Scene 视图**（与 Game 视图并排）
2. **运行游戏**
3. **观察绘制的Gizmos**：
   - 🟡 **黄色线框球**：手部当前位置
   - 🟢 **绿色线框立方体**：目标方块位置
   - 🔴 **红色连线**：从手部到目标的距离

---

## 🛠️ 调试工具

### Unity Inspector 配置

找到 `HandGuide` 对象 → `JewelHandGuide` 组件：

```
【调试工具】
✅ Show Debug Info: true        // 显示Gizmos和详细日志
✅ Use World Position: false    // 如果位置不对，尝试改为 true
```

### 右键菜单命令

在 Inspector 中，右键点击 `JewelHandGuide` 组件：

1. **刷新手部位置** - 手动重新计算位置
2. **打印调试信息** - 输出所有相关信息到控制台

---

## 📊 理解调试信息

### 控制台输出示例

```
========== JewelHandGuide 调试信息 ==========

手部 RectTransform:
  - 父物体: Canvas
  - 锚点Min: (0.5, 0.5)
  - 锚点Max: (0.5, 0.5)
  - Pivot: (0.5, 0.5)
  - AnchoredPosition: (120.5, -345.2)
  - LocalPosition: (120.5, -345.2, 0)
  - WorldPosition: (540.5, 454.8, 0)

Canvas 信息:
  - Canvas名称: Canvas
  - RenderMode: ScreenSpaceOverlay
  - UI Scale Mode: ScaleWithScreenSize
  - Reference Resolution: (1080, 1920)
  - Match: 0.5

游戏板信息:
  - 容器名称: BoardContainer
  - CellWidth: 60.0
  - CellHeight: 60.0
  - BoardWidth: 720.0
  - BottomOffsetY: -694.0

引导配置:
  - GuideRow: 3
  - GuideStartColumn: 6
  - GuideEndColumn: 7
  - XOffset: 0
  - YOffset: 0
  - AutoAdjustParent: true
  - UseWorldPosition: false
```

### 关键检查点

#### ✅ 1. 锚点必须一致
```
手部锚点: (0.5, 0.5) ✅
游戏板锚点: (0.5, 0.5) ✅
```

如果不一致，开启 `Auto Adjust Parent = true` 重新运行。

#### ✅ 2. 父物体必须相同
```
手部父物体: Canvas ✅
游戏板父物体: Canvas ✅
```

如果不一致，手动调整或开启 `Auto Adjust Parent`。

#### ✅ 3. Canvas Scaler 配置
```
UI Scale Mode: ScaleWithScreenSize ✅
Reference Resolution: (1080, 1920) 或其他合理值 ✅
```

---

## 🔧 常见问题解决

### 问题 1: 手部位置完全错乱

**解决方案A**：使用世界坐标模式

```
JewelHandGuide 组件：
✅ Use World Position: true
```

**解决方案B**：检查Canvas设置

1. 选中Canvas
2. 确认 `Canvas Scaler` 配置：
   - UI Scale Mode: `Scale With Screen Size`
   - Reference Resolution: `1080 x 1920`
   - Screen Match Mode: `Match Width Or Height`
   - Match: `0.5`

### 问题 2: 手部位置有轻微偏差

**使用偏移量微调**：

```
X Offset: 手部在左边 → +50，手部在右边 → -50
Y Offset: 手部在下面 → +50，手部在上面 → -50
```

**建议步进值**：每次调整 10-50 个单位

### 问题 3: 手部位置在不同分辨率下不一致

**检查清单**：
- [ ] `Auto Adjust Parent = true`
- [ ] 锚点设置为中心 (0.5, 0.5)
- [ ] Canvas Scaler 正确配置
- [ ] 手部和游戏板在同一父物体下

**如果仍然不对**，尝试：
```
Use World Position: true
```

### 问题 4: Scene视图看不到Gizmos

**开启Gizmos**：
1. Scene 视图右上角
2. 确认 `Gizmos` 按钮已开启
3. 确认 `Show Debug Info = true`

---

## 🎯 手动调试步骤

### 方法 1: 实时调试模式

1. **开启调试**：
   ```
   Show Debug Info: true
   Use World Position: true
   ```

2. **运行游戏**

3. **在 Scene 视图中观察**：
   - 手部（黄色球）应该移动到目标（绿色框）
   - 红线应该很短或消失

4. **如果还有偏差**，调整 `X/Y Offset`

### 方法 2: 静态位置测试

1. **暂停游戏**

2. **在 Inspector 中手动设置位置**：
   ```
   Hand Rect Transform:
   - Anchored Position: (目标X, 目标Y)
   ```

3. **从控制台复制目标位置**

4. **继续游戏测试**

### 方法 3: 使用 Unity 工具

1. **选中 HandGuide 对象**

2. **在 Scene 视图中**：
   - 使用移动工具 (W) 手动调整位置
   - 观察 `Anchored Position` 的值变化

3. **记录正确的位置值**

4. **在脚本中应用偏移量**：
   ```
   X Offset = 正确X - 计算X
   Y Offset = 正确Y - 计算Y
   ```

---

## 📐 位置计算公式

### 局部坐标计算（默认方式）

```csharp
targetX = ((GuideStartColumn + GuideEndColumn) / 2f) * cellWidth 
         + (cellWidth / 2f) 
         - (boardWidth / 2f) 
         + XOffset

targetY = bottomOffsetY 
         + (GuideRow * cellHeight) 
         + (cellHeight / 2f) 
         + YOffset
```

### 世界坐标计算（备用方式）

```csharp
// 1. 计算局部位置
Vector3 localPos = new Vector3(targetX, targetY, 0)

// 2. 转换为世界坐标
Vector3 worldPos = boardContainer.TransformPoint(localPos)

// 3. 设置世界位置
handRectTransform.position = worldPos
```

---

## 🧪 测试不同分辨率

### Unity 编辑器测试

1. **Game 视图** → 分辨率下拉菜单

2. **测试以下分辨率**：
   - iPhone 8 (750x1334) - 16:9
   - iPhone 13 (1170x2532) - 19.5:9
   - Samsung Galaxy (1440x3200) - 20:9
   - iPad (2048x2732) - 4:3

3. **每次切换后检查**：
   - 手部位置是否正确
   - Gizmos 绿色框是否对齐

### 真机测试

如果编辑器测试正常，但真机不对：

1. **Build 到真机**
2. **使用 Unity Remote** 实时调试
3. **记录设备信息**：
   - 设备型号
   - 屏幕分辨率
   - 偏差方向和距离

---

## 💡 推荐配置

### 配置 A：自动调整（推荐）

```
Auto Adjust Parent: true
Use World Position: false
Show Debug Info: true
X Offset: 0
Y Offset: 0
```

**适用场景**：大多数情况

### 配置 B：世界坐标（备用）

```
Auto Adjust Parent: true
Use World Position: true
Show Debug Info: true
X Offset: 0
Y Offset: 0
```

**适用场景**：
- 复杂的Canvas层级
- 多个Canvas
- 局部坐标不准确时

### 配置 C：手动微调

```
Auto Adjust Parent: true
Use World Position: false
Show Debug Info: false
X Offset: 根据实测调整
Y Offset: 根据实测调整
```

**适用场景**：
- 位置基本正确，只需微调
- 已经找到准确的偏移值

---

## 📋 完整调试清单

运行这个检查清单来诊断问题：

### Unity 配置
- [ ] Canvas 使用 `Scale With Screen Size`
- [ ] Reference Resolution 设置合理（如 1080x1920）
- [ ] HandGuide 和 BoardContainer 在同一父物体下
- [ ] HandGuide 的锚点为 (0.5, 0.5)
- [ ] `Auto Adjust Parent = true`

### 组件检查
- [ ] `JewelHandGuide` 组件正确添加
- [ ] `JewelGameManager` 正确引用 HandGuide
- [ ] `BoardManager` 正确初始化
- [ ] Hand Image 有正确的 Sprite

### 调试工具
- [ ] `Show Debug Info = true`
- [ ] 运行游戏后查看控制台日志
- [ ] Scene 视图中看到 Gizmos
- [ ] 绿色框在目标方块上
- [ ] 黄色球跟随手部

### 位置调整
- [ ] 尝试 `Use World Position = true`
- [ ] 调整 `X/Y Offset` 微调位置
- [ ] 在不同分辨率下测试
- [ ] 在真机上验证

---

## 🆘 如果还是不行

### 收集以下信息：

1. **控制台完整日志**（复制 "打印调试信息" 的输出）

2. **截图**：
   - Game 视图（手部位置）
   - Scene 视图（Gizmos）
   - Inspector（JewelHandGuide 配置）
   - Inspector（Canvas Scaler 配置）

3. **测试结果**：
   - `Use World Position = false` 的位置
   - `Use World Position = true` 的位置
   - 偏差距离和方向

4. **设备信息**：
   - 测试分辨率
   - Canvas Reference Resolution
   - 实际屏幕分辨率

---

## ✨ 快速解决方案

### 90% 的情况可以这样解决：

1. **开启世界坐标模式**：
   ```
   Use World Position: true
   ```

2. **如果还有偏差，微调**：
   ```
   X Offset: ±50
   Y Offset: ±50
   ```

3. **完成！**

---

希望这个调试指南能帮你解决手部位置问题！🎯













